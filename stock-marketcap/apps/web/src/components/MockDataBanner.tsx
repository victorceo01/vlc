export function MockDataBanner() {
  return (
    <div className="bg-amber-100 text-amber-900 text-xs sm:text-sm text-center px-3 py-1.5 border-b border-amber-200">
      ⚠️ Demo data only — all figures are <strong>mock/seed data</strong>, not live
      prices or investment advice.
    </div>
  );
}
